coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 1%
        if_ci_failed: error
    patch:
      default:
        target: 80%
        threshold: 1%

comment:
  layout: "header, diff, files"
  behavior: default

github_checks:
  annotations: true
