name: Network Integration Tests

on:
  schedule:
    - cron: '0 2 * * *'  # Daily at 2 AM UTC
  workflow_dispatch:

jobs:
  network-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: "1.91.0"
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "omnia-network-tests"
      - name: Run omnia-network tests
        run: cargo test -p omnia-network --verbose
      - name: Run omnia-consensus tests
        run: cargo test -p omnia-consensus --verbose
      - name: Run network integration tests (omnia-network)
        run: cargo test -p omnia-network --test network_integration_test --features network -- --ignored
        continue-on-error: true
